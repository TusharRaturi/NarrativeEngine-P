// Tier mapping lives in @narrative/engine (shared with the desktop app).
// Re-exported here so existing imports keep working.
export { mapTier, mapTierLegacy, validateBands } from '@narrative/engine';
export type { LegacyDiceConfig } from '@narrative/engine';
