// Archetype envelope tables live in @narrative/engine (shared with the
// mobile app). Re-exported here so existing imports keep working.
export { ENVELOPES, MODIFIERS, GROUP_KEYS } from '@narrative/engine';
export type { AxisSpread, AxisEnvelope, GroupEnvelope, AxisModifier, GroupModifiers } from '@narrative/engine';
